{
  "name": "SugarCubes",
  "version": "0.9.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js",
    "audit:standards": "python scripts/standards_audit.py && node scripts/standards-audit.mjs",
    "lint": "eslint \"web/**/*.js\"",
    "format": "prettier --write \"web/**/*.js\"",
    "check": "python -m black . --exclude \"node_modules|cubes\" && python -m ruff check . --exclude node_modules,cubes && npm run audit:standards && npm run format && npm run lint && node scripts/run-tests.mjs",
    "release": "semantic-release"
  },
  "keywords": [],
  "author": "",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/commit-analyzer": "^13.0.1",
    "@semantic-release/exec": "^7.1.0",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/release-notes-generator": "^14.1.1",
    "eslint": "^8.56.0",
    "eslint-config-prettier": "^9.1.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "prettier": "^3.3.3",
    "semantic-release": "^25.0.3"
  },
  "private": true
}
